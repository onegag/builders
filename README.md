To build project with Maven run (without tests):
```text
mvn clean install -DskipTests
```
(with tests):
```text
mvn clean install 
```
To build project with Gradle run (without tests):
```text
gradle build -x test
```
(with tests):
```text
gradle build
```
